---
name: kaka-cover
description: 为“咔咔的老吴”及同类商业教育内容制作统一风格的卡通封面。适用于先生成四张无字小样供筛选，再将选中稿放大并制作 AI 设计标题版的工作流，支持多种画布比例与自定义尺寸。
---

# 咔咔封面

制作一组可筛选、可延续角色身份、最终同时交付无字版和标题版的封面。涉及生成或编辑位图时，使用当前环境可用的图像生成能力；有 `imagegen` skill 时先读取并遵守它。

## 开始前

从用户输入中确定：

- 本期主题或标题
- 希望画面表达的冲突、动作和主体
- 画布比例或最终尺寸
- 是否使用老吴角色

同时判断最能说明主题的业务背景。背景不是装饰，应帮助观众立即识别内容属于 Meta、Google、电商、线下门店或其他业务场景。选择背景时读取 [references/background-selection.md](references/background-selection.md)。

尺寸未指定时默认 `16:9`，最终输出 `1920x1080`。常用选项：

| 用途 | 比例 | 建议最终尺寸 |
|---|---:|---:|
| YouTube 横版封面 | 16:9 | 1920x1080 |
| 方形社交图 | 1:1 | 1600x1600 |
| 竖版信息流 | 4:5 | 1600x2000 |
| Shorts / Reels | 9:16 | 1080x1920 |

用户也可以给出自定义宽高。保持用户给定比例，不擅自裁切主体。

## 固定工作流

### 第一轮：四张无字小样

1. 一次准备四个真正不同的构图方向，例如镜头角度、主体站位、动作瞬间或视觉隐喻不同；不要只换颜色。四稿都要使用与主题匹配的业务背景。
2. 每个方向单独调用一次图像生成，输出共四张小样。不要用同一调用的随机变体代替四个设计方向。
3. 小样严格使用目标比例。预览尺寸建议：16:9 用 `960x540`，其他比例按长边约 960 像素等比缩放。
4. 小样不放视频标题、角标、期数或装饰性文案。用户明确要求的场景内标签可以保留。
5. 编号展示 `A / B / C / D`，简短说明每张的视觉重点，然后等待用户选中一张。此时不要提前放大或加标题。

### 第二轮：选中稿与 AI 标题

1. 把选中小样当作不可变的视觉底稿。保留构图、人物身份、动作、表情、服装、颜色和光线，不重新解释主题。
2. 从原始生成文件制作目标尺寸的高清无字版。不要从压缩过的小样二次放大。
3. 再以同一张高清无字版为基础制作标题版。标题必须由 AI 按画面主题设计成完整字效，而不是简单叠加系统粗体。
4. 标题设计应有明确层级、配色逻辑、材质和空间关系，并适合缩略图阅读。默认参考 `assets/ai-title-treatment-reference.png` 的漫画重拳字效：重点词放大、红蓝阵营分色、金色视觉中心和克制的爆裂冲击形。用户指定其他方向时再调整。
5. 逐字核对标题。文字错误时只重做标题区域，保持底图不变；不要用错误文字交付。
6. 默认交付两张：高清无字版、高清标题版。用户要求更多字效时，再基于同一底图提供变体。

## 视觉规则

需要老吴时，读取 [references/visual-style.md](references/visual-style.md)，并使用其中指定的参考图。

核心不变量：

- 老吴是年轻、可爱的 Q 版卡通形象：头大身小、完全光头、没有两侧头发、圆形黑框眼镜。
- 穿白色 MUJI 风小立领衬衫，领口是窄立领，最上方扣子扣紧；禁止翻领、领带和领结。
- 角色材质优先采用精致立体 3D 卡通质感：有体积、皮肤或布料纹理、柔和高光和接触阴影，不做扁平 2D 人物。
- 保留原系列简洁有力的外轮廓收束，但描边不能压掉 3D 体积。
- 常用背景为暖白纸张卡片、轻微旧纸或水彩边缘、克制的米色边框；主体冲突清楚，背景不遮挡人物。
- 需要标题时提前为标题留出足够的干净区域；无字稿本身仍须完整成立。

## 参考图使用

参考图只控制指定属性，不照抄其中的主题或文字：

- `assets/laowu-character-reference.png`：老吴身份、比例、眼镜、光头、衬衫和领口。
- `assets/3d-monster-material-reference.png`：立体怪兽的体积、皮肤纹理、角、牙齿和棚拍光效。
- `assets/paper-watercolor-layout-reference.png`：暖白纸张、水彩边缘、留白与边框气质。
- `assets/meta-dashboard-background-reference.png`：Meta 广告后台作为业务背景时的界面占比和弱化方式。
- `assets/selected-cover-no-text-reference.png`：成熟封面的主体大小、冲突强度和缩略图可读性。
- `assets/ai-title-treatment-reference.png`：当前选定的漫画重拳标题材质、红蓝阵营关系、金色重点词和爆裂冲击形。

生成提示中明确写出每张参考图的角色，避免模型把参考图里的内容和文字复制到新主题中。

## 输出与命名

使用可读文件名并保留版本：

- `topic-draft-a.png` 至 `topic-draft-d.png`
- `topic-final-no-text.png`
- `topic-final-title.png`

不要覆盖用户已有文件。交付时给出可点击的绝对路径，并显示四张小样或最终标题版供用户直接比较。
